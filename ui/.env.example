# Local dev (npm run dev) only — Docker Compose injects these from the root .env.
# The proxy and middleware read these; keep them in sync with the root .env.
DASHBOARD_PASSWORD=changeme
DASHBOARD_JWT_SECRET=change-me-to-a-long-random-string
API_URL=http://localhost:8000
